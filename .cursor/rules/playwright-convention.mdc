---
alwaysApply: false
description: How this project writes Playwright tests — locators, waiting, POM, auth, data, API, mocking, accessibility, assertions, and anti-flake. Apply to all test work under tests/.
globs: tests/**
---
# Playwright Conventions
## Locators role/label first; testid escape hatch; never CSS/XPath
## Waiting web-first assertions only; never waitForTimeout
## Page Objects one per page; locators in constructor; no assertions in POMs
## Auth storageState via the setup project; API-login for speed
## Data unique data (Date.now()); every test cleans up after itself
## API request fixture for setup/teardown and contract checks
## Mocking route/fulfill for 500/503/timeout/empty/malformed
## Accessibility axe scan (wcag2a/aa) on key pages + a keyboard-nav check
## Assertions web-first; soft for multi-check; visual where it earns it
## Anti-flake CI-only retries; isolation; page.clock; pinned locale/timezone

Each rule below shows the standard, then a ✅ good and ❌ bad example. When in doubt, prefer the example a *user* (or a screen reader) would recognize over the one coupled to markup.

**QA mission:** tests encode acceptance criteria (`@qa-mission`). When the app violates AC, the test **should** fail — do not weaken assertions, skip checks, or change expectations to match broken behavior (except documented demo guardrails in `pom-conventions`). Notify the human after failures; file Jira or fix tests only with their approval.

## Refusals (hard stops)

When a request would cross one of these lines, **refuse** — stop work, state the conflict, and route to the human (`@qa-mission`). Do not comply silently or pick a "less bad" workaround.

| Refuse to… | Why it's a stop |
|------------|-----------------|
| **Weaken, delete, or replace an assertion** so a failing test passes | Green CI must mean the app meets AC — not that the test was silenced. Escalate: Jira (app bug) or an approved POM patch (test issue). |
| **Heal or patch locators/tests** when triage classified the failure as a **real app bug** | Healing hides defects. Route to `bug-reporter` / `@jira-bug-reporter`; wait for human approval before any test-layer change. |
| **Replace a role/label/text/testid locator with CSS or XPath** | Couples tests to markup; bypasses the accessibility contract. Re-discover via the a11y tree (`getByRole` → `getByLabel` → `getByText` → `getByTestId`). |
| **Add `waitForTimeout` or other fixed sleeps** | Guesses duration instead of waiting on reality. Use web-first `expect(...)` or action auto-waiting. |
| **Hardcode credentials, tokens, or passwords** in specs, POMs, or config | Secrets belong in env (`process.env`, `.env`); auth uses `storageState` from the setup project. |
| **Use TypeScript `any`** in tests, POMs, fixtures, or helpers | Erases type safety where flakiness and bad locators hide. Use proper types or `unknown` + narrowing. |

If the user insists after you raise the conflict, still do not comply — report the refusal and wait for explicit human override aligned with `@qa-mission`.

## Tagging strategy — layered tags on every `test()`

Every `test()` belongs to the **regression suite** and carries `@regression`. Add slice tags (`@smoke`, `@sanity`, `@api`, `@destructive`) when the test should run in a faster CI gate. Tags live on individual tests — **never** on `test.describe()`.

| Tag | Purpose | When to add |
|-----|---------|-------------|
| `@regression` | Full suite membership | **Required on every test** — regression = all tests |
| `@smoke` | PR gate (~5 min) | **Write-path CRUD** — one proven create, read/list, update, delete per release surface |
| `@sanity` | Post-push health (~15 min) | Smoke plus UI shells, alternate happy paths, live list refresh, auth gate, basic validation |
| `@api` | API layer | Primary subject is `request` — setup, teardown, or contract checks without browser UI |
| `@destructive` | Shared/global state | Mutates locale, roles, feature flags, or app settings — **not** self-cleaned test records |

**`@destructive` rules**

- Reserve `@destructive` for tests that change **shared or global** app state (locale, roles, flags, settings). A test that creates unique data and tears down only its own records keeps `@regression` plus `@smoke` or `@sanity` only.
- Run destructive tests serially: `npm run test:destructive` (uses `--workers=1`).
- Any state-mutating test — especially `@destructive` — **must** revert in `test.afterEach` or `test.afterAll` (restore locale, re-enable flag, delete temp role, etc.).

✅ **Good**

```tsx
test('TC-001: Programs page loads', { tag: ['@regression', '@smoke'] }, async ({ page }) => {
  await page.goto('/programs');
  await expect(page.getByRole('heading', { name: 'Programs' })).toBeVisible();
});

test('TC-002: Program is created', { tag: ['@regression', '@sanity'] }, async ({ page }) => {
  // ...
});

test('edge case only in full regression', { tag: '@regression' }, async ({ page }) => {
  // ...
});

test('changes org locale to fr-CA', { tag: ['@regression', '@destructive'] }, async ({ page }) => {
  await settings.setLocale('fr-CA');
  await expect(page.getByText('Programmes')).toBeVisible();
});

test.afterEach(async () => {
  await settings.revertLocale(); // required — restores shared state
});
```

❌ **Bad**

```tsx
test.describe('Programs', { tag: '@smoke' }, () => { /* tag on describe — forbidden */ });

test('creates a program', { tag: '@smoke' }, async () => {}); // missing @regression

test('disables billing flag', { tag: ['@regression', '@destructive'] }, async ({ page }) => {
  await settings.disableBilling();
  // no afterEach/afterAll revert — poisons the next test
});
```

**Run a slice**

```bash
npm run test:smoke        # @smoke (subset of regression)
npm run test:sanity       # @sanity (subset of regression)
npm run test:regression   # full suite — every test has @regression
npm run test:api
npm run test:destructive  # --workers=1
```

**CI**

| Trigger | Workflow job | Command |
|---------|--------------|---------|
| Push | Sanity | `npm run test:sanity` |
| Pull request | Smoke | `npm run test:smoke` |
| Manual (`workflow_dispatch`) | Regression | `npm run test:regression` |

### Smoke & sanity scope (risk-based)

Programs module risks ranked by **blast radius × change frequency**. Smoke keeps only **proven write/read paths**; sanity adds **surrounding happy paths** without edge-case depth.

| Risk | Impact if broken | Smoke | Sanity |
|------|------------------|:-----:|:------:|
| Programs list unreadable (auth/render) | Total — cannot manage data | DS-5 TC-001 | DS-5 TC-001, TC-002 |
| Create program fails | High — blocks all setup | DS-1 TC-002 | DS-1 TC-001, TC-002, TC-005 |
| Edit program fails | High — stale data | DS-2 TC-002 | DS-2 TC-001, TC-002, TC-003 |
| Delete program fails | Medium — data accumulates | DS-4 TC-001 | DS-4 TC-001, TC-002 |
| List stale after CRUD | Medium — wrong UI state | — | DS-5 TC-004 |
| Unauthenticated access | Medium — security | — | DS-5 TC-010 |
| Basic name validation | Medium — bad data entry | — | DS-3 TC-005 |
| Special chars / XSS / duplicates | Lower for PR gate | — | — (regression only) |
| Empty-state UX | Lower on push | — | DS-5 TC-002, TC-007 |

**Smoke (4 tests — full CRUD proof, PR gate)**

| Spec | TC | Why |
|------|----|-----|
| `ds5-program-list-display` | TC-001 | Read path: list renders names + descriptions (auth via setup) |
| `ds1-create-program` | TC-002 | Create path: program saved and visible in list |
| `ds2-edit-program` | TC-002 | Update path: renamed program reflected immediately |
| `ds4-delete-program` | TC-001 | Delete path: confirmed removal from list |

**Not smoke:** form-only checks (DS-1/DS-2 TC-001), validation edge cases (DS-3 TC-001 special characters), cancel flows, empty states — those belong in sanity or regression.

**Sanity (14 tests — smoke ⊂ sanity)**

All smoke tests, plus:

| Spec | TC | Why |
|------|----|-----|
| `ds1-create-program` | TC-001, TC-005 | Creation modal shell; name-only create variant |
| `ds2-edit-program` | TC-001, TC-003 | Edit modal prefilled; description-only update |
| `ds3-program-name-validation` | TC-005 | Basic unique-name happy path (not charset edge cases) |
| `ds4-delete-program` | TC-002 | Cancel delete — accidental deletion guard |
| `ds5-program-list-display` | TC-002, TC-004, TC-007, TC-010 | Empty state; list refresh after create; blank description; login redirect |

Tag assignments live in `scripts/apply-strategy-tags.js`. Re-apply after adding tests: `node scripts/apply-strategy-tags.js`.

## 1. Locators — user-facing, in priority order

Reach for locators top-to-bottom: `getByRole` → `getByLabel`/`getByPlaceholder` → `getByText` → `getByTestId` → (last resort) CSS/XPath, flagged for review.

✅ **Good**

```tsx
await page.getByRole('button', { name: 'New Program' }).click();
await page.getByLabel('Program Name').fill('Web Development 2026');
await page.getByRole('row', { name: /Web Development 2026/ }).getByRole('button', { name: 'Edit' }).click();
```

❌ **Bad**

```tsx
await page.locator('.btn.btn-primary').click();            // couples to CSS classes
await page.locator('#root > div > div:nth-child(3) input').fill('x'); // breaks on any layout change
await page.locator('//button[2]').click();                 // XPath by position — meaningless and brittle
```
**Why:** role/label locators survive restyling and refactors, and they double as an accessibility check — if `getByRole('button', { name: 'Create' })` finds it, the control has an accessible name. `getByTestId` is the escape hatch when nothing user-facing fits; CSS/XPath is a smell.

## 2. Waiting — web-first assertions only, never fixed timeouts

Never `waitForTimeout`. `expect()` retries until the condition holds or the timeout expires.

✅ **Good**

```tsx
await page.getByRole('button', { name: 'Save' }).click();
await expect(page.getByText('Program updated')).toBeVisible();
await expect(page.getByRole('button', { name: 'Save' })).toBeEnabled();
```

❌ **Bad**

```tsx
await page.getByRole('button', { name: 'Save' }).click();
await page.waitForTimeout(2000);                 // a bet: too short = flaky, too long = slow
expect(await page.getByText('Program updated').isVisible()).toBe(true); // snapshot, no retry
```

**Why:** a fixed wait guesses a duration; a web-first assertion waits for *reality*. `isVisible()` returns a boolean once and never retries — use `expect(locator).toBeVisible()` instead.

## 3. Page Object Model — locators in, assertions out

One POM per page/component. Locators live in the constructor; methods perform user actions; **no assertions in a POM** — those belong in the test, which describes intent.

✅ **Good**

```tsx
// pages/program-form.page.ts
export class ProgramFormPage {
  constructor(private page: Page) {}
  readonly name = this.page.getByLabel('Program Name');
  readonly create = this.page.getByRole('button', { name: 'Create' });

  async createProgram(name: string) {
    await this.name.fill(name);
    await this.create.click();
  }
}

// tests/program-create.spec.ts
await form.createProgram(unique('Web Dev'));
await expect(list.row(name)).toBeVisible();        // assertion lives in the test
```

❌ **Bad**

```tsx
export class ProgramFormPage {
  async createProgram(name: string) {
    await this.page.fill('#name', name);
    await this.page.click('.submit');
    await expect(this.page.getByText('created')).toBeVisible(); // assertion buried in the POM
  }
}
```

**Why:** assertions in POMs hide what a test verifies and make POMs un-reusable across tests that expect different outcomes. The test should read like the intent; the POM is the *how*.

## 4. Auth — log in once with storageState; never per test

Authenticate once in a setup project, save `storageState`, and let every test inherit it. Use API login when the UI login isn't what's under test.

✅ **Good**

```tsx
// tests/auth.setup.ts
setup('authenticate', async ({ page }) => {
  await page.goto('/login');
  await page.getByLabel('Email').fill(process.env.DIDAXIS_EMAIL!);
  await page.getByLabel('Password').fill(process.env.DIDAXIS_PASSWORD!);
  await page.getByRole('button', { name: 'Sign In' }).click();
  await page.context().storageState({ path: 'playwright/.auth/user.json' });
});
// config: main project has dependencies: ['setup'], use: { storageState: 'playwright/.auth/user.json' }
```

❌ **Bad**

```tsx
test.beforeEach(async ({ page }) => {       // logs in before EVERY test — slow, flaky
  await page.goto('/login');
  await page.getByLabel('Email').fill('admin@didaxis.studio'); // hardcoded credential
  await page.getByLabel('Password').fill('hunter2');
  await page.getByRole('button', { name: 'Sign In' }).click();
});
```

**Why:** per-test UI login multiplies runtime and adds a flaky dependency to every test. The auth file is git-ignored and regenerated each run (locally and in CI). Credentials come from `process.env`, never hardcoded.

## 5. Test data — factories, invalid sets, env, enums

Organize data by intent under `test-data/`:

| Kind | Location | Use |
|------|----------|-----|
| Happy-path payloads | `test-data/factories/` | Faker-backed builders with unique suffixes |
| Invalid / boundary inputs | `test-data/invalid/*.ts` | Curated static sets (whitespace, XSS, over-max-length) |
| Secrets and URLs | `process.env` | `DIDAXIS_URL`, tokens, credentials — never in specs |
| Repeated paths and copy | `test-data/enums/` | Routes, status labels, toast messages used in 2+ places |

Every test still creates **unique, self-cleaning** data and tears down after itself (`Date.now()` suffix + API cleanup). Factories wrap Faker; they do not replace teardown.

✅ **Good**

```tsx
// test-data/factories/program.factory.ts
import { faker } from '@faker-js/faker';

export function buildProgram(overrides: Partial<{ name: string; description: string }> = {}) {
  const suffix = Date.now();
  return {
    name: `${faker.company.catchPhrase()} ${suffix}`,
    description: faker.lorem.paragraph({ min: 1, max: 2 }),
    ...overrides,
  };
}

// test-data/invalid/program.invalid.ts
export const WHITESPACE_ONLY_NAMES = ['', '   ', '\t\t\t'] as const;

// tests/program-create.spec.ts
import { buildProgram } from '../test-data/factories/program.factory';
import { WHITESPACE_ONLY_NAMES } from '../test-data/invalid/program.invalid';
import { AppRoute } from '../test-data/enums/routes';

test('rejects whitespace-only name', { tag: '@regression' }, async ({ page }) => {
  const { name } = buildProgram({ name: WHITESPACE_ONLY_NAMES[1] });
  await page.goto(AppRoute.Programs);
  // ...
});

test.afterEach(async ({ request }) => {
  await cleanupProgramsByPrefix(request, 'Web Dev-');
});
```

❌ **Bad**

```tsx
test('creates a program', async ({ page }) => {
  await form.createProgram('Web Development');   // static name → collisions in parallel
  const url = 'https://test.didaxis.studio/programs'; // hardcoded env URL
});                                              // no cleanup → pollutes the next run
```

**Why:** Faker factories keep happy-path data varied and unique; static invalid sets document the exact boundary cases under test. Enums and env vars stop copy-paste drift. API cleanup remains faster and more reliable than UI teardown.

## 6. API testing — use the `request` fixture for setup, teardown, and contracts

Use the API for state you don't want to build through the UI, for teardown, and for contract checks. Mix API setup with UI assertions to keep tests fast and focused.

✅ **Good**

```tsx
test('list shows a program created via API', async ({ page, request }) => {
  const res = await request.post('/api/programs', { data: { name: unique('API Prog') } });
  expect(res.ok()).toBeTruthy();
  const { id, name } = await res.json();
  await page.goto('/programs');
  await expect(page.getByRole('row', { name })).toBeVisible();
  await request.delete(`/api/programs/${id}`);    // teardown via API
});
```

❌ **Bad**

```tsx
test('list shows a program', async ({ page }) => {
  // builds 10 prerequisite programs by clicking through the UI — slow and fragile
  for (let i = 0; i < 10; i++) await form.createProgram(`Prog ${i}`);
});
```

**Why:** the UI is for testing the UI, not for manufacturing prerequisite data. API setup/teardown is an order of magnitude faster and far less flaky.

## 7. Network mocking — make edge cases deterministic

Mock the failures you can't trigger on demand: `500`, `503`, timeout, empty list, malformed payload, slow response. Mock the case you're forcing — never mock the thing you're trying to test.

✅ **Good**

```tsx
test('shows an error when save fails', async ({ page }) => {
  await page.route('**/api/programs', route =>
    route.fulfill({ status: 503, body: 'Service Unavailable' }));
  await form.createProgram(unique('Web Dev'));
  await expect(page.getByText('Something went wrong')).toBeVisible();
});

test('handles an empty list', async ({ page }) => {
  await page.route('**/api/programs', route =>
    route.fulfill({ status: 200, body: '[]' }));
  await page.goto('/programs');
  await expect(page.getByText('No programs yet')).toBeVisible();
});
```

❌ **Bad**

```tsx
test('create works', async ({ page }) => {
  await page.route('**/api/programs', route =>
    route.fulfill({ status: 200, body: JSON.stringify({ id: 1, name: 'x' }) }));
  await form.createProgram('x');                 // mocks the very endpoint under test → proves nothing
  await expect(page.getByText('created')).toBeVisible();
});
```

**Why:** happy-path tests prove the app works when the server behaves; mocking proves it survives when the server misbehaves — which production guarantees. Use `routeFromHAR` to replay a recorded session when you need realistic payloads.

## 8. Accessibility — axe is the floor, keyboard + names are the rest

Run an automated scan on key pages, and add the checks axe can't make: keyboard operability and accessible names. Axe catches roughly a third of issues; you own the other two-thirds.

✅ **Good**

```tsx
import AxeBuilder from '@axe-core/playwright';

test('programs page has no a11y violations', async ({ page }) => {
  await page.goto('/programs');
  const results = await new AxeBuilder({ page }).withTags(['wcag2a', 'wcag2aa']).analyze();
  expect(results.violations).toEqual([]);
});

test('new-program flow is keyboard operable', async ({ page }) => {
  await page.goto('/programs');
  await page.keyboard.press('Tab');
  await expect(page.getByRole('button', { name: 'New Program' })).toBeFocused();
  await page.keyboard.press('Enter');
  await expect(page.getByRole('dialog', { name: 'New Program' })).toBeVisible();
});
```

❌ **Bad**

```tsx
test('page looks fine', async ({ page }) => {
  await page.goto('/programs');
  // eyeballed once in dev; no scan, no keyboard path, no assertion
});
// or: an icon-only button with no accessible name —
await page.locator('button.icon-edit').click();   // getByRole('button', { name: 'Edit' }) would FAIL here, which is the bug
```

**Why:** a control a screen reader can't name is broken the same way a 500 is broken — you just had no test that noticed. If your `getByRole(... name ...)` locators work, your accessible names are sane; if they don't, that failure *is* the accessibility bug. Scope scans with `.include()/.exclude()` for third-party widgets.

## 9. Assertions — web-first by default, soft for multi-check, visual where it earns it

Prefer auto-retrying web-first assertions. Use `expect.soft` to collect several checks in one run. Use visual snapshots sparingly, masking dynamic regions.

✅ **Good**

```tsx
// collect all failures instead of stopping at the first
await expect.soft(page.getByRole('heading', { name: 'Programs' })).toBeVisible();
await expect.soft(page.getByRole('button', { name: 'New Program' })).toBeEnabled();

// visual, with dynamic content masked
await expect(page).toHaveScreenshot('programs.png', {
  mask: [page.getByRole('cell', { name: /Updated/ })],
});
```

❌ **Bad**

```tsx
const text = await page.getByRole('heading').textContent();
expect(text).toContain('Programs');               // no retry; races the render

await expect(page).toHaveScreenshot();             // unmasked timestamps → fails every run
```

**Why:** non-retrying assertions race the UI; web-first assertions wait. Unmasked visual snapshots flake on any dynamic content. Soft assertions turn three reruns into one informative failure.

## 10. Anti-flake — flake is a configuration choice

Retries in CI only, full isolation, pinned locale/timezone, controlled time, trace on first retry.

✅ **Good**

```tsx
// playwright.config.ts
export default defineConfig({
  fullyParallel: true,
  retries: process.env.CI ? 2 : 0,        // local runs fail loudly; CI tolerates a transient blip
  use: {
    baseURL: process.env.DIDAXIS_URL,
    trace: 'on-first-retry',
    locale: 'en-CA',
    timezoneId: 'America/Toronto',         // kills date/locale flake
  },
});

// freeze time so a "created 2 minutes ago" label is deterministic
await page.clock.install({ time: new Date('2026-06-08T12:00:00') });
```

❌ **Bad**

```tsx
export default defineConfig({
  retries: 5,                              // hides real flake behind brute force
  workers: 1,                              // serial "fix" for tests that share state
});
// tests that read `new Date()` and assert on "today" → break at midnight / across timezones
```

**Why:** retries buy a green build, not a fixed one — a test that only passes on retry is a bug with a snooze button; track those. Pinned locale/timezone and `page.clock` remove whole classes of time-dependent flake. Cranking `workers: 1` to stop collisions hides the real problem: tests that aren't isolated.
