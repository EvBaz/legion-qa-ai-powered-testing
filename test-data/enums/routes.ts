/** App routes referenced in 2+ specs or POMs — keep paths out of inline strings. */
export enum AppRoute {
  Login = '/login',
  Programs = '/programs',
  Dashboard = '/dashboard',
  Settings = '/settings',
}
